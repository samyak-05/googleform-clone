create database google;

use google;

create table admins(
aname varchar(40),
apass varchar(20)
);

create table info(

user_name varchar(25),
phone_no bigint,
email varchar(50),
city varchar(20),
state varchar(30),
pincode int,
college varchar(70)
);











